## Upload image and reduce and resize it using python Fastapi REST API

install require packages
```
pip install fastapi
pip install uvicorn
pip install python-multipart
pip install Image
pip install opencv-python
pip install uuid


Start server 
`````````````````
uvicorn main:app --reload